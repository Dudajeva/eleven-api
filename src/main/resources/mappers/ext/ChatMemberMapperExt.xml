<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.adzc.elevenapi.mapper.ChatMemberMapper">

    <select id="find" resultType="org.adzc.elevenapi.domain.ChatMember">
        select * from chat_member where conversation_id=#{cid} and user_id=#{uid} limit 1
    </select>

    <select id="exists" resultType="boolean">
        select 1 from chat_member where conversation_id=#{cid} and user_id=#{uid} limit 1
    </select>

    <update id="updateLastRead">
        update chat_member
        set last_read_message_id = greatest(coalesce(last_read_message_id,0), #{lastRead})
        where conversation_id=#{cid} and user_id=#{uid}
    </update>

    <select id="getLastRead" resultType="long">
        select coalesce(last_read_message_id,0) from chat_member where conversation_id=#{cid} and user_id=#{uid}
    </select>

    <insert id="insertMember">
        insert into chat_member (conversation_id, user_id, joined_at)
        values (#{cid}, #{uid}, now())
    </insert>

</mapper>
